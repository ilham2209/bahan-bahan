apt-get update

apt-get install repo git-core gnupg flex bison gperf build-essential zip curl zlib1g-dev gcc-multilib g++-multilib libc6-dev-i386 lib32ncurses5-dev x11proto-core-dev libx11-dev lib32z-dev libgl1-mesa-dev libxml2-utils xsltproc unzip fontconfig
export TEMPORARY_DISABLE_PATH_RESTRICTIONS=true
apt install bc
add

# Maintainer
PRODUCT_PROPERTY_OVERRIDES += \
    ro.havoc.maintainer=nama
apt install libssl-dev
eror sepolicy
SELINUX_IGNORE_NEVERALLOWS := true
CURRENT_BUILD_TYPE :=gapps


Join nangis sourceforge

1. Kontak bagas minta gabung

2. Di terminal sftp <username>@frs.sourceforge.net

3. cd /home/frs/project/nangis/

4. mkdir <nama folder>, atau ls dulu liat²

5. cd <nama folder>

6. put out/target/blablabla.zip (alamat rom u)

7. Dah lah tunggu

TARGET_KERNEL_CLANG_COMPILE := true

bring up
cd device
git checkout -b branch
git push origin branch
git add . && git commit
git push
